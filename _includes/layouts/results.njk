---
layout: layouts/base.njk
pagination:
    data: results.f2021
    size: 1
    alias: results
permalink: "{{ link }}/{{ results.team | slug }}/"
templateClass: result-page
---
<article class="oil-page">

<h1 class="post-title">{{ results.team }}</h1>
<figure class="video-container">
<iframe loading="lazy" src="{{ results.video }}" width="100%" height="100%" frameborder="0" webkitallowfullscreen="" mozallowfullscreen="" allowfullscreen=""></iframe>
</figure>

<section>
<img src="{{ oil.img }}" alt="{{ oil.title }} essential oil bottle" class=" oil-bottle">
<div>
{%- for p in oil.description  -%}
<p>{{ p | safe }}</p>

{%- endfor %}
</div>
</section>

{% if results.award %}
<p>* <em class='legal'>These statements have not been evaluated by the Food and Drug Administration. This product is not intended to diagnose, treat, cure, or prevent any disease.</em></p>
{% endif %}



{%- set nextPost = collections.posts | getNextCollectionItem(page) %}
{%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}
{%- if nextPost or previousPost %}
<hr>
<ul>
  {%- if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  {%- if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
</ul>
{%- endif %}
</article>
